{{- $fullname := include "url-utilities-admin.fullname" . -}}
url-utilities-admin was deployed as release {{ .Release.Name }} in namespace {{ .Release.Namespace }}.

Image:     {{ include "url-utilities-admin.image" . }}
CORE_URL:  {{ .Values.config.CORE_URL }}

To check status:
  kubectl -n {{ .Release.Namespace }} rollout status deploy/{{ $fullname }}

{{- if .Values.ingress.enabled }}

Ingress enabled. Once DNS and TLS are ready, browse to:
{{- range .Values.ingress.hosts }}
  https://{{ .host }}
{{- end }}
{{- else }}

Ingress is disabled. Port-forward for local access:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ $fullname }} 4000:{{ .Values.service.port }}
  open http://localhost:4000/urls
{{- end }}
