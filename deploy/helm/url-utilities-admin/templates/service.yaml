apiVersion: v1
kind: Service
metadata:
  name: {{ include "url-utilities-admin.fullname" . }}
  labels:
    {{- include "url-utilities-admin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "url-utilities-admin.selectorLabels" . | nindent 4 }}
